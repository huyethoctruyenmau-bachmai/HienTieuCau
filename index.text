<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#0b6b3a">
  <title>Hiến tiểu cầu Bạch Mai</title>
  <style>
    :root{
      --primary:#0b6b3a;
      --primary-dark:#07532d;
      --primary-soft:#e9f5ee;
      --accent:#d71920;
      --text:#17211b;
      --muted:#68756d;
      --line:#dbe4de;
      --surface:#ffffff;
      --bg:#f3f7f4;
      --warning:#a86100;
      --danger:#b3261e;
      --success:#137333;
      --shadow:0 12px 34px rgba(20,54,35,.12);
      --radius:18px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;min-height:100%;font-family:Arial,sans-serif;color:var(--text);background:var(--bg)}
    body{padding-bottom:env(safe-area-inset-bottom)}
    button,input,select,textarea{font:inherit}
    button{cursor:pointer}
    button:disabled{cursor:not-allowed;opacity:.55}
    .hidden{display:none!important}
    .app-shell{min-height:100vh}
    .topbar{
      position:sticky;top:0;z-index:30;
      background:linear-gradient(135deg,var(--primary-dark),var(--primary));
      color:#fff;padding:calc(12px + env(safe-area-inset-top)) 18px 12px;
      box-shadow:0 8px 24px rgba(0,0,0,.12)
    }
    .brand{max-width:1160px;margin:auto;display:flex;align-items:center;gap:14px}
    .brand-mark{
      width:52px;height:52px;flex:0 0 52px;border-radius:50%;
      background:#fff;color:var(--primary);display:grid;place-items:center;
      font-weight:800;font-size:17px;border:3px solid rgba(255,255,255,.42)
    }
    .brand-copy{min-width:0;flex:1}
    .brand-copy h1{font-size:20px;margin:0 0 4px;letter-spacing:.2px}
    .brand-copy p{margin:0;font-size:12px;line-height:1.45;opacity:.92}
    .staff-button{border:1px solid rgba(255,255,255,.55);background:rgba(255,255,255,.12);color:#fff;border-radius:12px;padding:10px 12px;font-weight:700;white-space:nowrap}
    .hero{max-width:1160px;margin:22px auto 0;padding:0 18px}
    .hero-card{
      background:linear-gradient(135deg,#fff,#f3fff8);border:1px solid var(--line);
      border-radius:24px;box-shadow:var(--shadow);padding:24px;display:grid;
      grid-template-columns:minmax(0,1.5fr) minmax(280px,.7fr);gap:22px;align-items:center
    }
    .hero h2{margin:0 0 10px;font-size:30px;color:var(--primary-dark)}
    .hero p{margin:0;color:var(--muted);line-height:1.65}
    .hero-info{display:grid;gap:10px}
    .info-chip{display:flex;gap:10px;align-items:flex-start;background:#fff;border:1px solid var(--line);border-radius:14px;padding:12px}
    .info-chip b{display:block;font-size:13px;margin-bottom:4px}
    .info-chip span{font-size:13px;color:var(--muted);line-height:1.45}
    .main{max-width:1160px;margin:20px auto;padding:0 18px 36px}
    .wizard-card{background:var(--surface);border:1px solid var(--line);border-radius:24px;box-shadow:var(--shadow);overflow:hidden}
    .steps{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:0;border-bottom:1px solid var(--line);background:#fbfdfb}
    .step-tab{position:relative;padding:16px 8px;text-align:center;color:#7c8780;font-size:12px;font-weight:700}
    .step-tab:not(:last-child)::after{content:"";position:absolute;top:25px;right:-15%;width:30%;height:2px;background:#dfe7e2}
    .step-dot{width:34px;height:34px;margin:0 auto 7px;border-radius:50%;display:grid;place-items:center;background:#e6ebe8;color:#5c665f;font-size:14px}
    .step-tab.active{color:var(--primary)}
    .step-tab.active .step-dot,.step-tab.done .step-dot{background:var(--primary);color:#fff}
    .step-tab.done{color:var(--primary-dark)}
    .panel{padding:26px;display:none}
    .panel.active{display:block}
    .panel-header{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;margin-bottom:20px}
    .panel-header h3{margin:0 0 6px;font-size:24px}
    .panel-header p{margin:0;color:var(--muted);line-height:1.55}
    .section-title{font-size:16px;margin:22px 0 12px;color:var(--primary-dark)}
    .grid-2{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}
    .grid-3{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}
    .field{display:flex;flex-direction:column;gap:7px;min-width:0}
    .field label{font-size:13px;font-weight:700;color:#3f4b43}
    .field small{color:var(--muted);line-height:1.4}
    .input,.select,.textarea{
      width:100%;height:50px;border:1px solid #becbc3;border-radius:13px;background:#fff;
      color:var(--text);padding:0 14px;outline:none;transition:.18s
    }
    .textarea{height:auto;min-height:100px;padding:13px;resize:vertical}
    .input:focus,.select:focus,.textarea:focus{border-color:var(--primary);box-shadow:0 0 0 3px rgba(11,107,58,.12)}
    .vi-date-wrap{position:relative;height:50px}
    .vi-date-display{position:absolute;inset:0;z-index:1;padding-right:46px}
    .vi-date-native{position:absolute;inset:0;z-index:2;width:100%;height:100%;opacity:0;cursor:pointer}
    .date-icon{position:absolute;right:13px;top:50%;transform:translateY(-50%);z-index:1;color:var(--primary);font-size:22px;pointer-events:none}
    .actions{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-top:24px}
    .actions-right{display:flex;gap:10px;margin-left:auto}
    .btn{height:48px;border-radius:13px;border:0;padding:0 18px;font-weight:700;display:inline-flex;align-items:center;justify-content:center;gap:8px}
    .btn-primary{background:var(--primary);color:#fff;box-shadow:0 8px 18px rgba(11,107,58,.18)}
    .btn-primary:hover{background:var(--primary-dark)}
    .btn-secondary{background:#edf3ef;color:var(--primary-dark)}
    .btn-outline{background:#fff;color:var(--primary);border:1px solid var(--primary)}
    .btn-danger{background:#fff0ef;color:var(--danger);border:1px solid #efc3bf}
    .notice{padding:14px 16px;border-radius:14px;line-height:1.55;font-size:14px}
    .notice-info{background:#edf7f1;color:#1b5135;border:1px solid #cbe6d5}
    .notice-warning{background:#fff7e8;color:#6e4700;border:1px solid #efd7a8}
    .notice-danger{background:#fff1f0;color:#7f1d17;border:1px solid #efc2be}
    .otp-box{max-width:520px;margin:0 auto;text-align:center}
    .otp-input{font-size:28px;letter-spacing:12px;text-align:center;font-weight:700}
    .otp-timer{font-size:34px;font-weight:700;color:var(--primary);margin:20px 0 4px}
    .slots{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}
    .slot{border:1px solid #b9c8bf;background:#fff;border-radius:14px;padding:14px;text-align:left;min-height:78px;transition:.16s}
    .slot:hover:not(:disabled){border-color:var(--primary);transform:translateY(-1px)}
    .slot.selected{background:var(--primary);color:#fff;border-color:var(--primary)}
    .slot.unavailable{background:#edf0ee;color:#8b948e}
    .slot strong{display:block;margin-bottom:6px}
    .slot small{font-size:12px}
    .question-list{display:grid;gap:14px}
    .question-group{border:1px solid var(--line);border-radius:16px;overflow:hidden}
    .question-group h4{margin:0;background:var(--primary-soft);padding:13px 16px;color:var(--primary-dark)}
    .question{padding:16px;border-top:1px solid var(--line)}
    .question:first-of-type{border-top:0}
    .question-text{font-weight:700;line-height:1.55;margin-bottom:12px}
    .choice-row{display:flex;gap:10px;flex-wrap:wrap}
    .choice{position:relative}
    .choice input{position:absolute;opacity:0}
    .choice label{display:flex;align-items:center;gap:8px;border:1px solid #c5d0c9;border-radius:12px;padding:10px 16px;cursor:pointer;background:#fff}
    .choice input:checked+label{background:var(--primary);border-color:var(--primary);color:#fff}
    .review-card{border:1px solid var(--line);border-radius:16px;overflow:hidden}
    .review-row{display:grid;grid-template-columns:180px minmax(0,1fr);gap:14px;padding:13px 16px;border-top:1px solid var(--line)}
    .review-row:first-child{border-top:0}
    .review-row b{color:#4b574f}
    .consent{display:flex;gap:10px;align-items:flex-start;margin-top:16px;padding:14px;border-radius:14px;background:#f7faf8;border:1px solid var(--line)}
    .consent input{margin-top:3px;width:18px;height:18px;accent-color:var(--primary)}
    .loading-overlay{position:fixed;inset:0;background:rgba(245,249,246,.88);z-index:100;display:grid;place-items:center;padding:20px}
    .loading-card{background:#fff;border-radius:18px;padding:24px 28px;box-shadow:var(--shadow);text-align:center;min-width:240px}
    .spinner{width:38px;height:38px;border:4px solid #dce8e0;border-top-color:var(--primary);border-radius:50%;margin:0 auto 12px;animation:spin .8s linear infinite}
    @keyframes spin{to{transform:rotate(360deg)}}
    .toast{position:fixed;left:50%;bottom:calc(24px + env(safe-area-inset-bottom));transform:translateX(-50%) translateY(30px);z-index:120;background:#243129;color:#fff;padding:12px 16px;border-radius:12px;box-shadow:var(--shadow);opacity:0;pointer-events:none;transition:.2s;max-width:min(90vw,620px);text-align:center}
    .toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
    .modal-backdrop{position:fixed;inset:0;z-index:80;background:rgba(18,27,21,.62);display:none;align-items:center;justify-content:center;padding:18px}
    .modal-backdrop.show{display:flex}
    .modal{width:min(680px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:20px;box-shadow:0 28px 70px rgba(0,0,0,.26)}
    .modal-head{position:sticky;top:0;background:var(--primary);color:#fff;padding:16px 18px;display:flex;justify-content:space-between;align-items:center;z-index:2}
    .modal-head h3{margin:0;font-size:20px}
    .icon-btn{width:40px;height:40px;border-radius:10px;border:0;background:rgba(255,255,255,.14);color:#fff;font-size:24px}
    .modal-body{padding:20px}
    .success-layout{text-align:center}
    .success-icon{width:66px;height:66px;border-radius:50%;background:#e5f5eb;color:var(--success);display:grid;place-items:center;font-size:34px;margin:0 auto 12px}
    .success-layout h3{margin:0 0 8px;color:var(--primary-dark)}
    #qrCode{display:flex;justify-content:center;margin:18px auto;background:#fff;padding:14px;border:1px solid var(--line);width:max-content;border-radius:12px}
    .ticket{background:#f6faf7;border:1px dashed #a9bcae;border-radius:14px;padding:14px;text-align:left;line-height:1.7}
    .admin-shell{min-height:100vh;background:#eef4f0}
    .admin-top{position:sticky;top:0;z-index:20;background:#123c28;color:#fff;padding:14px 18px;display:flex;align-items:center;gap:14px}
    .admin-top h2{margin:0;font-size:18px;flex:1}
    .admin-main{max-width:1280px;margin:auto;padding:20px}
    .admin-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px;margin-bottom:18px}
    .metric{background:#fff;border:1px solid var(--line);border-radius:16px;padding:16px;box-shadow:0 5px 15px rgba(25,50,35,.05)}
    .metric span{display:block;color:var(--muted);font-size:12px;margin-bottom:8px}
    .metric strong{font-size:28px;color:var(--primary-dark)}
    .admin-card{background:#fff;border:1px solid var(--line);border-radius:18px;padding:18px;margin-bottom:18px}
    .filter-row{display:grid;grid-template-columns:180px 180px 190px minmax(180px,1fr) auto;gap:10px;align-items:end}
    .table-wrap{overflow:auto;border:1px solid var(--line);border-radius:14px}
    table{width:100%;border-collapse:collapse;min-width:980px;background:#fff}
    th,td{padding:12px 10px;border-bottom:1px solid var(--line);text-align:left;font-size:13px;vertical-align:top}
    th{background:#f0f6f2;color:#334239;position:sticky;top:0}
    .status{display:inline-flex;border-radius:999px;padding:5px 9px;font-size:11px;font-weight:700;white-space:nowrap;background:#edf2ee;color:#415048}
    .status.CONFIRMED{background:#eaf2ff;color:#2458a5}
    .status.CHECKED_IN{background:#fff3d8;color:#8a5700}
    .status.COMPLETED{background:#e6f5eb;color:#176b35}
    .status.CANCELLED,.status.NO_SHOW{background:#ffebea;color:#9b2520}
    .status.DEFERRED{background:#fff0db;color:#985d00}
    .risk.REVIEW{color:var(--warning);font-weight:700}
    .footer{text-align:center;color:#7a857e;font-size:12px;padding:16px 18px 30px}
    @media (max-width:900px){
      .hero-card{grid-template-columns:1fr}
      .slots{grid-template-columns:repeat(3,minmax(0,1fr))}
      .admin-grid{grid-template-columns:repeat(2,minmax(0,1fr))}
      .filter-row{grid-template-columns:repeat(2,minmax(0,1fr))}
    }
    @media (max-width:760px){
      body{background:#fff}
      .topbar{padding-left:14px;padding-right:14px}
      .brand-mark{width:44px;height:44px;flex-basis:44px;font-size:14px}
      .brand-copy h1{font-size:16px}
      .brand-copy p{font-size:10px}
      .staff-button{padding:8px 9px;font-size:11px}
      .hero{margin-top:12px;padding:0 12px}
      .hero-card{padding:17px;border-radius:18px;box-shadow:none}
      .hero h2{font-size:23px}
      .main{margin-top:12px;padding:0 0 24px}
      .wizard-card{border-left:0;border-right:0;border-radius:0;box-shadow:none}
      .steps{position:sticky;top:80px;z-index:10;overflow:hidden}
      .step-tab{padding:12px 3px;font-size:9px}
      .step-dot{width:28px;height:28px;font-size:12px}
      .step-tab:not(:last-child)::after{top:21px}
      .panel{padding:20px 14px}
      .panel-header h3{font-size:21px}
      .grid-2,.grid-3{grid-template-columns:1fr}
      .slots{grid-template-columns:repeat(2,minmax(0,1fr))}
      .slot{min-height:72px;padding:12px}
      .review-row{grid-template-columns:1fr;gap:5px}
      .actions{position:sticky;bottom:0;background:rgba(255,255,255,.96);padding:12px 0 calc(12px + env(safe-area-inset-bottom));z-index:8}
      .actions .btn{flex:1}
      .actions-right{width:100%;margin:0}
      .admin-main{padding:12px}
      .admin-grid{grid-template-columns:repeat(2,minmax(0,1fr))}
      .filter-row{grid-template-columns:1fr}
      .modal-backdrop{padding:0;align-items:flex-end}
      .modal{border-radius:20px 20px 0 0;max-height:92vh}
    }
  </style>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js" defer></script>
</head>
<body>
  <div id="publicApp" class="app-shell">
    <header class="topbar">
      <div class="brand">
        <div class="brand-mark" aria-label="Logo tạm">BM</div>
        <div class="brand-copy">
          <h1 id="brandTitle">HIẾN TIỂU CẦU BẠCH MAI</h1>
          <p id="brandOrg">BỆNH VIỆN BẠCH MAI · VIỆN HUYẾT HỌC VÀ TRUYỀN MÁU BẠCH MAI</p>
        </div>
        <button class="staff-button" type="button" onclick="openAdminLogin()">Dành cho nhân viên</button>
      </div>
    </header>

    <section class="hero">
      <div class="hero-card">
        <div>
          <h2>Đăng ký hiến tiểu cầu</h2>
          <p>Chủ động chọn ngày và khung giờ phù hợp. Thông tin khai báo giúp nhân viên y tế chuẩn bị tiếp nhận thuận lợi hơn. Việc đăng ký trực tuyến không thay thế khám và đánh giá điều kiện hiến tại Bệnh viện Bạch Mai.</p>
        </div>
        <div class="hero-info">
          <div class="info-chip"><div>📍</div><div><b>Địa điểm</b><span id="heroLocation">Đang tải...</span></div></div>
          <div class="info-chip"><div>🕘</div><div><b>Thời gian tiếp nhận</b><span id="heroWorkingHours">Đang tải...</span></div></div>
          <div class="info-chip"><div>☎</div><div><b>Hỗ trợ</b><span id="heroPhone">Đang cập nhật</span></div></div>
        </div>
      </div>
    </section>

    <main class="main">
      <section class="wizard-card">
        <div class="steps" id="stepsNav">
          <div class="step-tab active" data-step="1"><div class="step-dot">1</div><span>Thông tin</span></div>
          <div class="step-tab" data-step="2"><div class="step-dot">2</div><span>Xác thực</span></div>
          <div class="step-tab" data-step="3"><div class="step-dot">3</div><span>Khung giờ</span></div>
          <div class="step-tab" data-step="4"><div class="step-dot">4</div><span>Sức khỏe</span></div>
          <div class="step-tab" data-step="5"><div class="step-dot">5</div><span>Xác nhận</span></div>
        </div>

        <section class="panel active" data-panel="1">
          <div class="panel-header">
            <div><h3>Thông tin đăng ký</h3><p>Chọn ngày dự kiến hiến và xác thực bằng số điện thoại cá nhân.</p></div>
          </div>
          <div class="grid-2">
            <div class="field">
              <label for="registerDate">Ngày dự kiến hiến *</label>
              <div class="vi-date-wrap">
                <input id="registerDateDisplay" class="input vi-date-display date-vi-input" type="text" readonly placeholder="dd/mm/yyyy">
                <input id="registerDate" class="vi-date-native" type="date" aria-label="Ngày dự kiến hiến">
                <span class="date-icon">▣</span>
              </div>
              <small>Hệ thống chỉ cho phép chọn ngày đang mở đăng ký.</small>
            </div>
            <div class="field">
              <label for="phone">Số điện thoại *</label>
              <input id="phone" class="input" type="tel" inputmode="numeric" maxlength="12" placeholder="Ví dụ: 0912345678" autocomplete="tel">
              <small>Mã OTP sẽ được gửi đến số điện thoại này.</small>
            </div>
          </div>
          <div id="turnstileContainer" class="hidden" style="margin-top:16px"></div>
          <div class="notice notice-info" style="margin-top:18px">Vui lòng khai báo trung thực. Quyết định đủ điều kiện hiến tiểu cầu được thực hiện sau khi khám và xét nghiệm tại cơ sở tiếp nhận.</div>
          <div class="actions"><div></div><div class="actions-right"><button class="btn btn-primary" type="button" onclick="requestOtp()">Gửi mã OTP →</button></div></div>
        </section>

        <section class="panel" data-panel="2">
          <div class="panel-header"><div><h3>Xác thực số điện thoại</h3><p>Nhập mã OTP gồm 6 chữ số. Mã chỉ có hiệu lực trong thời gian ngắn.</p></div></div>
          <div class="otp-box">
            <input id="otp" class="input otp-input" type="text" inputmode="numeric" maxlength="6" autocomplete="one-time-code" placeholder="••••••">
            <div id="otpTimer" class="otp-timer">03:00</div>
            <div id="otpHint" style="color:var(--muted);font-size:13px">Mã OTP đang được gửi tới điện thoại.</div>
            <div class="actions">
              <button class="btn btn-secondary" type="button" onclick="goToStep(1)">← Sửa thông tin</button>
              <div class="actions-right">
                <button id="resendOtpBtn" class="btn btn-outline" type="button" onclick="requestOtp(true)" disabled>Gửi lại</button>
                <button class="btn btn-primary" type="button" onclick="verifyOtp()">Xác nhận →</button>
              </div>
            </div>
          </div>
        </section>

        <section class="panel" data-panel="3">
          <div class="panel-header"><div><h3>Thông tin người hiến và khung giờ</h3><p>Kiểm tra thông tin cá nhân, sau đó chọn một khung giờ còn chỗ.</p></div></div>
          <div class="grid-3">
            <div class="field"><label for="fullName">Họ và tên *</label><input id="fullName" class="input" type="text" maxlength="100" placeholder="Nhập đầy đủ họ tên"></div>
            <div class="field">
              <label for="birthDate">Ngày sinh *</label>
              <div class="vi-date-wrap">
                <input id="birthDateDisplay" class="input vi-date-display date-vi-input" type="text" readonly placeholder="dd/mm/yyyy">
                <input id="birthDate" class="vi-date-native" type="date" aria-label="Ngày sinh">
                <span class="date-icon">▣</span>
              </div>
            </div>
            <div class="field"><label for="gender">Giới tính *</label><select id="gender" class="select"><option value="">Chọn giới tính</option><option value="MALE">Nam</option><option value="FEMALE">Nữ</option><option value="OTHER">Khác</option></select></div>
            <div class="field"><label for="citizenIdLast4">4 số cuối CCCD</label><input id="citizenIdLast4" class="input" inputmode="numeric" maxlength="4" placeholder="Không bắt buộc"></div>
            <div class="field"><label for="province">Tỉnh/Thành phố</label><input id="province" class="input" maxlength="100" placeholder="Nơi đang sinh sống"></div>
            <div class="field"><label for="email">Email</label><input id="email" class="input" type="email" maxlength="120" placeholder="Không bắt buộc"></div>
          </div>
          <h4 class="section-title">Khung giờ ngày <span id="slotDateText"></span></h4>
          <div id="slots" class="slots"></div>
          <div id="slotsEmpty" class="notice notice-warning hidden">Ngày này chưa có khung giờ khả dụng. Vui lòng quay lại chọn ngày khác.</div>
          <div class="actions"><button class="btn btn-secondary" type="button" onclick="goToStep(2)">← Quay lại</button><div class="actions-right"><button class="btn btn-primary" type="button" onclick="continueToQuestions()">Tiếp tục →</button></div></div>
        </section>

        <section class="panel" data-panel="4">
          <div class="panel-header"><div><h3>Khai báo sức khỏe</h3><p>Trả lời đầy đủ các câu hỏi. Các nội dung cần làm rõ sẽ được nhân viên y tế xem xét khi tiếp nhận.</p></div></div>
          <div class="notice notice-warning" style="margin-bottom:16px">Bộ câu hỏi trong bản 1.1 là cấu trúc mẫu. Trước khi vận hành thật, Viện cần phê duyệt nội dung câu hỏi và quy tắc đánh giá.</div>
          <div id="questionList" class="question-list"></div>
          <div class="actions"><button class="btn btn-secondary" type="button" onclick="goToStep(3)">← Quay lại</button><div class="actions-right"><button class="btn btn-primary" type="button" onclick="continueToReview()">Xem lại thông tin →</button></div></div>
        </section>

        <section class="panel" data-panel="5">
          <div class="panel-header"><div><h3>Xác nhận đăng ký</h3><p>Kiểm tra kỹ thông tin trước khi gửi đăng ký.</p></div></div>
          <div id="reviewCard" class="review-card"></div>
          <label class="consent"><input id="consent" type="checkbox"><span>Tôi xác nhận thông tin đã khai là đúng và hiểu rằng đăng ký trực tuyến không đồng nghĩa chắc chắn đủ điều kiện hiến. Quyết định cuối cùng thuộc về nhân viên y tế sau khi khám và xét nghiệm.</span></label>
          <div class="actions"><button class="btn btn-secondary" type="button" onclick="goToStep(4)">← Quay lại</button><div class="actions-right"><button class="btn btn-primary" type="button" onclick="submitRegistration()">XÁC NHẬN ĐĂNG KÝ</button></div></div>
        </section>
      </section>
    </main>
    <footer class="footer">Phiên bản <span id="publicVersion">1.1</span> · Viện Huyết học và Truyền máu Bạch Mai</footer>
  </div>

  <div id="adminApp" class="admin-shell hidden">
    <div class="admin-top"><div class="brand-mark" style="width:42px;height:42px;flex-basis:42px">BM</div><h2>Quản lý đăng ký hiến tiểu cầu</h2><span id="adminUserName"></span><button class="staff-button" type="button" onclick="logoutAdmin()">Đăng xuất</button></div>
    <main class="admin-main">
      <div class="admin-card">
        <div class="filter-row" style="grid-template-columns:220px auto 1fr">
          <div class="field"><label>Ngày tổng quan</label><input id="dashboardDate" class="input" type="date"></div>
          <button class="btn btn-primary" type="button" onclick="loadAdminDashboard()">Tải dữ liệu</button>
          <button class="btn btn-outline" type="button" onclick="showPublicApp()" style="justify-self:end">← Về trang đăng ký</button>
        </div>
      </div>
      <section class="admin-grid">
        <div class="metric"><span>Tổng đăng ký</span><strong id="mTotal">0</strong></div>
        <div class="metric"><span>Đã xác nhận</span><strong id="mConfirmed">0</strong></div>
        <div class="metric"><span>Đã đến</span><strong id="mCheckin">0</strong></div>
        <div class="metric"><span>Hoàn thành</span><strong id="mCompleted">0</strong></div>
        <div class="metric"><span>Chỗ còn lại</span><strong id="mRemaining">0</strong></div>
      </section>
      <section class="admin-card">
        <h3 style="margin-top:0">Danh sách lịch hẹn</h3>
        <div class="filter-row">
          <div class="field"><label>Từ ngày</label><input id="filterFrom" class="input" type="date"></div>
          <div class="field"><label>Đến ngày</label><input id="filterTo" class="input" type="date"></div>
          <div class="field"><label>Trạng thái</label><select id="filterStatus" class="select"><option value="">Tất cả</option><option>CONFIRMED</option><option>CHECKED_IN</option><option>SCREENING</option><option>ELIGIBLE</option><option>IN_PROGRESS</option><option>COMPLETED</option><option>DEFERRED</option><option>CANCELLED</option><option>NO_SHOW</option></select></div>
          <div class="field"><label>Tìm kiếm</label><input id="filterKeyword" class="input" placeholder="Mã, họ tên, số điện thoại"></div>
          <button class="btn btn-primary" type="button" onclick="loadAppointments()">Tìm</button>
        </div>
        <div class="table-wrap" style="margin-top:14px">
          <table><thead><tr><th>Mã</th><th>Người đăng ký</th><th>Điện thoại</th><th>Ngày/giờ</th><th>Nguy cơ</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody id="appointmentBody"></tbody></table>
        </div>
      </section>
      <section class="admin-card" id="slotAdminCard">
        <h3 style="margin-top:0">Tạo khung giờ</h3>
        <div class="grid-3">
          <div class="field"><label>Từ ngày</label><input id="slotFrom" class="input" type="date"></div>
          <div class="field"><label>Đến ngày</label><input id="slotTo" class="input" type="date"></div>
          <div class="field"><label>Số người/khung</label><input id="slotCapacity" class="input" type="number" min="1" value="4"></div>
          <div class="field"><label>Giờ bắt đầu</label><input id="slotStart" class="input" type="time" value="07:00"></div>
          <div class="field"><label>Giờ kết thúc</label><input id="slotEnd" class="input" type="time" value="16:00"></div>
          <div class="field"><label>Số phút/khung</label><input id="slotInterval" class="input" type="number" min="30" step="30" value="60"></div>
        </div>
        <label class="consent" style="display:inline-flex"><input id="slotWeekend" type="checkbox"><span>Tạo cả thứ Bảy và Chủ nhật</span></label>
        <div class="actions"><div></div><button class="btn btn-primary" type="button" onclick="generateSlots()">Tạo khung giờ</button></div>
      </section>
    </main>
  </div>

  <div id="loading" class="loading-overlay hidden"><div class="loading-card"><div class="spinner"></div><b id="loadingText">Đang tải dữ liệu...</b></div></div>
  <div id="toast" class="toast"></div>

  <div id="successModal" class="modal-backdrop">
    <div class="modal">
      <div class="modal-head"><h3>Đăng ký thành công</h3><button class="icon-btn" type="button" onclick="closeModal('successModal')">×</button></div>
      <div class="modal-body success-layout">
        <div class="success-icon">✓</div>
        <h3>ĐĂNG KÝ HIẾN TIỂU CẦU THÀNH CÔNG</h3>
        <p id="successMessage" style="color:var(--muted)"></p>
        <div id="successTicket" class="ticket"></div>
        <div id="qrCode"></div>
        <div class="notice notice-info">Khi đến, vui lòng mang theo giấy tờ tùy thân và xuất trình mã QR này cho nhân viên tiếp nhận.</div>
        <div class="actions-right" style="justify-content:center;margin-top:16px"><button class="btn btn-outline" type="button" onclick="downloadQr()">Tải mã QR</button><button class="btn btn-primary" type="button" onclick="resetRegistration()">Đăng ký mới</button></div>
      </div>
    </div>
  </div>

  <div id="adminLoginModal" class="modal-backdrop">
    <div class="modal" style="max-width:460px">
      <div class="modal-head"><h3>ĐĂNG NHẬP NHÂN VIÊN</h3><button class="icon-btn" type="button" onclick="closeModal('adminLoginModal')">×</button></div>
      <div class="modal-body">
        <div class="field"><label>Tài khoản</label><input id="adminUsername" class="input" autocomplete="username"></div>
        <div class="field" style="margin-top:14px"><label>Mật khẩu</label><input id="adminPassword" class="input" type="password" autocomplete="current-password"></div>
        <button class="btn btn-primary" style="width:100%;margin-top:18px" type="button" onclick="loginAdmin()">ĐĂNG NHẬP</button>
      </div>
    </div>
  </div>

  <script>
    'use strict';

    const FRONTEND_VERSION = '1.1';
    const WEB_APP_URL = 'PASTE_YOUR_GOOGLE_APPS_SCRIPT_EXEC_URL_HERE';

    const state = {
      step: 1,
      app: {},
      questions: [],
      registerDate: '',
      phone: '',
      otpSessionId: '',
      publicToken: '',
      selectedSlot: null,
      slots: [],
      answers: {},
      otpSeconds: 0,
      otpTimerId: null,
      turnstileWidgetId: null,
      registration: null,
      adminToken: sessionStorage.getItem('bm_admin_token') || '',
      adminUser: JSON.parse(sessionStorage.getItem('bm_admin_user') || 'null')
    };

    document.addEventListener('DOMContentLoaded', init);

    async function init(){
      setupDateInputs();
      document.getElementById('publicVersion').textContent = FRONTEND_VERSION;
      const today = localIsoDate(new Date());
      document.getElementById('dashboardDate').value = today;
      document.getElementById('filterFrom').value = today;
      document.getElementById('filterTo').value = today;
      document.getElementById('slotFrom').value = today;
      document.getElementById('slotTo').value = today;

      if(WEB_APP_URL.includes('PASTE_YOUR')){
        showToast('Cần thay WEB_APP_URL trong index.html bằng URL Google Apps Script /exec.');
        return;
      }

      setLoading(true,'Đang tải cấu hình...');
      try{
        const data = await api('public_bootstrap',{});
        state.app = data.app || {};
        state.questions = data.questions || [];
        applyBranding();
        configureRegisterDate(data.serverDate);
        renderQuestions();
        initTurnstile();
      }catch(error){
        showToast(error.message || 'Không tải được cấu hình ứng dụng.');
      }finally{
        setLoading(false);
      }
    }

    async function api(action,data){
      const response = await fetch(WEB_APP_URL,{
        method:'POST',
        redirect:'follow',
        body:JSON.stringify({action,data})
      });
      if(!response.ok) throw new Error('Không kết nối được máy chủ.');
      const result = await response.json();
      if(!result.ok) {
        const error = new Error(result.error?.message || 'Có lỗi xảy ra.');
        error.code = result.error?.code || 'UNKNOWN';
        throw error;
      }
      return result.data;
    }

    function applyBranding(){
      document.getElementById('brandTitle').textContent = state.app.name || 'HIẾN TIỂU CẦU BẠCH MAI';
      document.getElementById('brandOrg').textContent = [state.app.organizationLine1,state.app.organizationLine2].filter(Boolean).join(' · ');
      document.getElementById('heroLocation').textContent = state.app.location || 'Đang cập nhật';
      document.getElementById('heroWorkingHours').textContent = state.app.workingHours || 'Theo lịch được công bố';
      document.getElementById('heroPhone').textContent = state.app.supportPhone || 'Đang cập nhật';
    }

    function configureRegisterDate(serverDate){
      const input = document.getElementById('registerDate');
      const min = new Date((serverDate || localIsoDate(new Date())) + 'T00:00:00');
      min.setDate(min.getDate()+1);
      const max = new Date(min);
      max.setDate(max.getDate() + Math.max(1,Number(state.app.maxBookingDays || 30)) - 1);
      input.min = localIsoDate(min);
      input.max = localIsoDate(max);
      input.value = localIsoDate(min);
      syncDateDisplay(input);
    }

    function setupDateInputs(){
      ['registerDate','birthDate'].forEach(id=>{
        const input = document.getElementById(id);
        if(!input) return;
        input.addEventListener('change',()=>syncDateDisplay(input));
        input.addEventListener('input',()=>syncDateDisplay(input));
      });
    }

    function syncDateDisplay(input){
      const display = document.getElementById(input.id+'Display');
      if(display) display.value = formatDateVi(input.value);
    }

    function formatDateVi(iso){
      if(!/^\d{4}-\d{2}-\d{2}$/.test(iso || '')) return '';
      const [y,m,d] = iso.split('-');
      return `${d}/${m}/${y}`;
    }

    function localIsoDate(date){
      const y=date.getFullYear();
      const m=String(date.getMonth()+1).padStart(2,'0');
      const d=String(date.getDate()).padStart(2,'0');
      return `${y}-${m}-${d}`;
    }

    function initTurnstile(){
      if(!state.app.turnstileSiteKey) return;
      const container = document.getElementById('turnstileContainer');
      container.classList.remove('hidden');
      const script = document.createElement('script');
      script.src='https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit';
      script.async=true;
      script.onload=()=>{
        state.turnstileWidgetId = turnstile.render(container,{sitekey:state.app.turnstileSiteKey,theme:'light'});
      };
      document.head.appendChild(script);
    }

    function getTurnstileToken(){
      if(!state.app.turnstileSiteKey || state.turnstileWidgetId===null) return '';
      return turnstile.getResponse(state.turnstileWidgetId) || '';
    }

    async function requestOtp(isResend=false){
      const date = document.getElementById('registerDate').value;
      const phone = document.getElementById('phone').value.trim();
      if(!date) return showToast('Vui lòng chọn ngày dự kiến hiến.');
      if(!/^0\d{9}$/.test(phone.replace(/\D/g,''))) return showToast('Số điện thoại chưa đúng định dạng.');

      setLoading(true,isResend?'Đang gửi lại OTP...':'Đang gửi OTP...');
      try{
        const data = await api('auth_requestOtp',{date,phone,turnstileToken:getTurnstileToken()});
        state.registerDate = date;
        state.phone = phone.replace(/\D/g,'');
        state.otpSessionId = data.otpSessionId;
        if(data.debugOtp){
          document.getElementById('otpHint').textContent = `Chế độ thử nghiệm: OTP là ${data.debugOtp}`;
          document.getElementById('otp').value = data.debugOtp;
        }else{
          document.getElementById('otpHint').textContent = 'Mã OTP đã được gửi tới điện thoại của bạn.';
        }
        startOtpTimer(Number(data.expiresInSeconds || 180),Number(data.resendAfterSeconds || 60));
        goToStep(2);
      }catch(error){
        showToast(error.message);
        if(state.turnstileWidgetId!==null && window.turnstile) turnstile.reset(state.turnstileWidgetId);
      }finally{setLoading(false)}
    }

    function startOtpTimer(seconds,resendAfter){
      clearInterval(state.otpTimerId);
      state.otpSeconds=seconds;
      const resendBtn=document.getElementById('resendOtpBtn');
      resendBtn.disabled=true;
      let elapsed=0;
      updateOtpTimer();
      state.otpTimerId=setInterval(()=>{
        state.otpSeconds=Math.max(0,state.otpSeconds-1);
        elapsed++;
        updateOtpTimer();
        if(elapsed>=resendAfter) resendBtn.disabled=false;
        if(state.otpSeconds<=0) clearInterval(state.otpTimerId);
      },1000);
    }

    function updateOtpTimer(){
      const m=String(Math.floor(state.otpSeconds/60)).padStart(2,'0');
      const s=String(state.otpSeconds%60).padStart(2,'0');
      document.getElementById('otpTimer').textContent=`${m}:${s}`;
    }

    async function verifyOtp(){
      const otp=document.getElementById('otp').value.trim();
      if(!/^\d{6}$/.test(otp)) return showToast('Vui lòng nhập đủ 6 số OTP.');
      setLoading(true,'Đang xác thực...');
      try{
        const data=await api('auth_verifyOtp',{otpSessionId:state.otpSessionId,otp});
        state.publicToken=data.token;
        clearInterval(state.otpTimerId);
        await loadDonorAndSlots();
        goToStep(3);
      }catch(error){showToast(error.message)}finally{setLoading(false)}
    }

    async function loadDonorAndSlots(){
      const [profile,slotData]=await Promise.all([
        api('donor_getProfile',{token:state.publicToken}),
        api('schedule_getSlots',{token:state.publicToken,date:state.registerDate})
      ]);
      if(profile.exists){
        document.getElementById('fullName').value=profile.fullName || '';
        document.getElementById('birthDate').value=profile.birthDate || '';
        syncDateDisplay(document.getElementById('birthDate'));
        document.getElementById('gender').value=profile.gender || '';
        document.getElementById('citizenIdLast4').value=profile.citizenIdLast4 || '';
        document.getElementById('province').value=profile.province || '';
        document.getElementById('email').value=profile.email || '';
      }
      state.slots=slotData.slots || [];
      state.selectedSlot=null;
      document.getElementById('slotDateText').textContent=formatDateVi(state.registerDate);
      renderSlots();
    }

    function renderSlots(){
      const wrap=document.getElementById('slots');
      const empty=document.getElementById('slotsEmpty');
      wrap.innerHTML='';
      empty.classList.toggle('hidden',state.slots.length>0);
      state.slots.forEach(slot=>{
        const button=document.createElement('button');
        button.type='button';
        button.className='slot'+(!slot.available?' unavailable':'')+(state.selectedSlot?.slotId===slot.slotId?' selected':'');
        button.disabled=!slot.available;
        button.innerHTML=`<strong>${escapeHtml(slot.startTime)} – ${escapeHtml(slot.endTime)}</strong><small>${slot.available?`Còn ${slot.remaining} chỗ`:'Không khả dụng'}</small>`;
        button.onclick=()=>{state.selectedSlot=slot;renderSlots()};
        wrap.appendChild(button);
      });
    }

    function continueToQuestions(){
      const fullName=document.getElementById('fullName').value.trim();
      const birthDate=document.getElementById('birthDate').value;
      const gender=document.getElementById('gender').value;
      if(!fullName) return showToast('Vui lòng nhập họ và tên.');
      if(!birthDate) return showToast('Vui lòng chọn ngày sinh.');
      if(!gender) return showToast('Vui lòng chọn giới tính.');
      if(!state.selectedSlot) return showToast('Vui lòng chọn khung giờ.');
      goToStep(4);
    }

    function renderQuestions(){
      const wrap=document.getElementById('questionList');
      wrap.innerHTML='';
      const groups={};
      state.questions.forEach(q=>{(groups[q.group] ||= []).push(q)});
      Object.entries(groups).forEach(([group,questions])=>{
        const section=document.createElement('section');
        section.className='question-group';
        section.innerHTML=`<h4>${escapeHtml(group)}</h4>`;
        questions.forEach(q=>section.appendChild(createQuestionElement(q)));
        wrap.appendChild(section);
      });
    }

    function createQuestionElement(q){
      const box=document.createElement('div');
      box.className='question';
      const required=q.required?' *':'';
      box.innerHTML=`<div class="question-text">${escapeHtml(q.text)}${required}</div>`;
      if(q.type==='YES_NO'){
        const choices=document.createElement('div');
        choices.className='choice-row';
        ['YES','NO'].forEach(value=>{
          const id=`${q.id}_${value}`;
          const item=document.createElement('div');
          item.className='choice';
          item.innerHTML=`<input type="radio" id="${id}" name="${q.id}" value="${value}"><label for="${id}">${value==='YES'?'Có':'Không'}</label>`;
          item.querySelector('input').addEventListener('change',e=>state.answers[q.id]=e.target.value);
          choices.appendChild(item);
        });
        box.appendChild(choices);
      }else{
        const textarea=document.createElement('textarea');
        textarea.className='textarea';
        textarea.placeholder='Nhập thông tin bổ sung';
        textarea.addEventListener('input',e=>state.answers[q.id]=e.target.value.trim());
        box.appendChild(textarea);
      }
      return box;
    }

    function continueToReview(){
      const missing=state.questions.filter(q=>q.required && (state.answers[q.id]===undefined || state.answers[q.id]===''));
      if(missing.length) return showToast('Vui lòng trả lời đầy đủ các câu hỏi bắt buộc.');
      renderReview();
      goToStep(5);
    }

    function renderReview(){
      const fullName=document.getElementById('fullName').value.trim();
      const birthDate=document.getElementById('birthDate').value;
      const genderText=document.getElementById('gender').selectedOptions[0]?.textContent || '';
      const flagged=state.questions.filter(q=>q.flagAnswer && String(state.answers[q.id]).toUpperCase()===String(q.flagAnswer).toUpperCase());
      document.getElementById('reviewCard').innerHTML=[
        ['Họ và tên',fullName],['Ngày sinh',formatDateVi(birthDate)],['Giới tính',genderText],['Số điện thoại',state.phone],['Ngày đăng ký',formatDateVi(state.registerDate)],['Khung giờ',`${state.selectedSlot.startTime} – ${state.selectedSlot.endTime}`],['Địa điểm',state.app.location || ''],['Khai báo cần xem xét',flagged.length?`${flagged.length} nội dung`:'Không có cảnh báo tự động']
      ].map(([label,value])=>`<div class="review-row"><b>${escapeHtml(label)}</b><span>${escapeHtml(value)}</span></div>`).join('');
      document.getElementById('consent').checked=false;
    }

    async function submitRegistration(){
      if(!document.getElementById('consent').checked) return showToast('Vui lòng xác nhận nội dung cam kết.');
      const payload={
        fullName:document.getElementById('fullName').value.trim(),
        birthDate:document.getElementById('birthDate').value,
        gender:document.getElementById('gender').value,
        citizenIdLast4:document.getElementById('citizenIdLast4').value.trim(),
        province:document.getElementById('province').value.trim(),
        email:document.getElementById('email').value.trim(),
        date:state.registerDate,
        slotId:state.selectedSlot.slotId,
        answers:state.answers
      };
      setLoading(true,'Đang ghi nhận đăng ký...');
      try{
        const data=await api('registration_create',{token:state.publicToken,payload});
        state.registration=data;
        localStorage.setItem('bm_last_registration',JSON.stringify({code:data.code,manageToken:data.manageToken}));
        showSuccess(data);
      }catch(error){
        showToast(error.message);
        if(error.code==='SLOT_FULL'){
          await loadDonorAndSlots();
          goToStep(3);
        }
      }finally{setLoading(false)}
    }

    function showSuccess(data){
      document.getElementById('successMessage').textContent=data.message || '';
      document.getElementById('successTicket').innerHTML=`<b>Mã đăng ký:</b> ${escapeHtml(data.code)}<br><b>Người hiến:</b> ${escapeHtml(data.fullName)}<br><b>Ngày:</b> ${formatDateVi(data.date)}<br><b>Khung giờ:</b> ${escapeHtml(data.startTime)} – ${escapeHtml(data.endTime)}<br><b>Địa điểm:</b> ${escapeHtml(data.location || '')}`;
      const qr=document.getElementById('qrCode');
      qr.innerHTML='';
      if(window.QRCode){
        new QRCode(qr,{text:data.qrText,width:230,height:230,correctLevel:QRCode.CorrectLevel.M});
      }else{
        qr.textContent='Không tải được thư viện tạo QR. Mã đăng ký: '+data.code;
      }
      openModal('successModal');
    }

    function downloadQr(){
      const qr=document.querySelector('#qrCode canvas') || document.querySelector('#qrCode img');
      if(!qr) return showToast('Chưa có ảnh QR để tải.');
      const url=qr.tagName==='CANVAS'?qr.toDataURL('image/png'):qr.src;
      const a=document.createElement('a');
      a.href=url;
      a.download=`QR_${state.registration?.code || 'HIENTIEUCAU'}.png`;
      a.click();
    }

    function resetRegistration(){
      closeModal('successModal');
      state.step=1;state.registerDate='';state.phone='';state.otpSessionId='';state.publicToken='';state.selectedSlot=null;state.slots=[];state.answers={};state.registration=null;
      document.querySelectorAll('input:not([type=date]):not([type=checkbox]):not([type=radio]), textarea').forEach(el=>{if(!el.closest('#adminApp')) el.value=''});
      document.querySelectorAll('#publicApp input[type=radio],#publicApp input[type=checkbox]').forEach(el=>el.checked=false);
      renderQuestions();
      configureRegisterDate(localIsoDate(new Date()));
      goToStep(1);
    }

    function goToStep(step){
      state.step=step;
      document.querySelectorAll('.panel').forEach(panel=>panel.classList.toggle('active',Number(panel.dataset.panel)===step));
      document.querySelectorAll('.step-tab').forEach(tab=>{
        const n=Number(tab.dataset.step);
        tab.classList.toggle('active',n===step);
        tab.classList.toggle('done',n<step);
        const dot=tab.querySelector('.step-dot');
        dot.textContent=n<step?'✓':String(n);
      });
      window.scrollTo({top:document.querySelector('.wizard-card').offsetTop-90,behavior:'smooth'});
    }

    function openAdminLogin(){
      if(state.adminToken && state.adminUser){showAdminApp();return}
      openModal('adminLoginModal');
      setTimeout(()=>document.getElementById('adminUsername').focus(),100);
    }

    async function loginAdmin(){
      const username=document.getElementById('adminUsername').value.trim();
      const password=document.getElementById('adminPassword').value;
      if(!username || !password) return showToast('Vui lòng nhập tài khoản và mật khẩu.');
      setLoading(true,'Đang đăng nhập...');
      try{
        const data=await api('account_login',{username,password});
        state.adminToken=data.token;state.adminUser=data.user;
        sessionStorage.setItem('bm_admin_token',data.token);
        sessionStorage.setItem('bm_admin_user',JSON.stringify(data.user));
        closeModal('adminLoginModal');
        showAdminApp();
        await loadAdminDashboard();
      }catch(error){showToast(error.message)}finally{setLoading(false)}
    }

    async function logoutAdmin(){
      try{if(state.adminToken) await api('account_logout',{token:state.adminToken})}catch(error){}
      state.adminToken='';state.adminUser=null;
      sessionStorage.removeItem('bm_admin_token');
      sessionStorage.removeItem('bm_admin_user');
      showPublicApp();
    }

    function showAdminApp(){
      document.getElementById('publicApp').classList.add('hidden');
      document.getElementById('adminApp').classList.remove('hidden');
      document.getElementById('adminUserName').textContent=state.adminUser?.fullName || '';
      const canConfigure=['SUPER_ADMIN','ADMIN'].includes(state.adminUser?.role);
      document.getElementById('slotAdminCard').classList.toggle('hidden',!canConfigure);
      window.scrollTo(0,0);
    }

    function showPublicApp(){
      document.getElementById('adminApp').classList.add('hidden');
      document.getElementById('publicApp').classList.remove('hidden');
      window.scrollTo(0,0);
    }

    async function loadAdminDashboard(){
      if(!state.adminToken) return openAdminLogin();
      const date=document.getElementById('dashboardDate').value || localIsoDate(new Date());
      setLoading(true,'Đang tải dữ liệu quản lý...');
      try{
        const data=await api('admin_getDashboard',{token:state.adminToken,date});
        document.getElementById('mTotal').textContent=data.total || 0;
        document.getElementById('mConfirmed').textContent=data.byStatus?.CONFIRMED || 0;
        document.getElementById('mCheckin').textContent=data.byStatus?.CHECKED_IN || 0;
        document.getElementById('mCompleted').textContent=data.byStatus?.COMPLETED || 0;
        document.getElementById('mRemaining').textContent=data.remaining || 0;
        document.getElementById('filterFrom').value=date;
        document.getElementById('filterTo').value=date;
        await loadAppointments(false);
      }catch(error){handleAdminError(error)}finally{setLoading(false)}
    }

    async function loadAppointments(showLoader=true){
      if(showLoader) setLoading(true,'Đang tải danh sách...');
      try{
        const data=await api('admin_listAppointments',{
          token:state.adminToken,
          fromDate:document.getElementById('filterFrom').value,
          toDate:document.getElementById('filterTo').value,
          status:document.getElementById('filterStatus').value,
          keyword:document.getElementById('filterKeyword').value.trim()
        });
        renderAppointments(data.items || []);
      }catch(error){handleAdminError(error)}finally{if(showLoader)setLoading(false)}
    }

    function renderAppointments(items){
      const body=document.getElementById('appointmentBody');
      if(!items.length){body.innerHTML='<tr><td colspan="7" style="text-align:center;color:#78847c">Không có dữ liệu phù hợp.</td></tr>';return}
      body.innerHTML=items.map(item=>`<tr><td><b>${escapeHtml(item.code)}</b></td><td>${escapeHtml(item.fullName)}<br><small>${formatDateVi(item.birthDate)}</small></td><td>${escapeHtml(item.phone)}</td><td>${formatDateVi(item.date)}<br>${escapeHtml(item.startTime)}–${escapeHtml(item.endTime)}</td><td><span class="risk ${escapeHtml(item.riskLevel)}">${escapeHtml(item.riskLevel)}</span></td><td><span class="status ${escapeHtml(item.status)}">${escapeHtml(item.status)}</span></td><td>${statusSelectHtml(item)}</td></tr>`).join('');
    }

    function statusSelectHtml(item){
      const editable=['SUPER_ADMIN','ADMIN','RECEPTION','SCREENING'].includes(state.adminUser?.role);
      if(!editable) return 'Chỉ xem';
      const options=['CONFIRMED','CHECKED_IN','SCREENING','ELIGIBLE','IN_PROGRESS','COMPLETED','DEFERRED','CANCELLED','NO_SHOW'];
      return `<select class="select" style="height:38px;min-width:150px" onchange="updateAppointmentStatus('${escapeAttr(item.appointmentId)}',this.value)">${options.map(s=>`<option ${s===item.status?'selected':''}>${s}</option>`).join('')}</select>`;
    }

    async function updateAppointmentStatus(appointmentId,status){
      setLoading(true,'Đang cập nhật trạng thái...');
      try{
        await api('admin_updateAppointmentStatus',{token:state.adminToken,appointmentId,status,notes:''});
        showToast('Đã cập nhật trạng thái.');
        await loadAdminDashboard();
      }catch(error){handleAdminError(error)}finally{setLoading(false)}
    }

    async function generateSlots(){
      setLoading(true,'Đang tạo khung giờ...');
      try{
        const data=await api('admin_generateSlots',{
          token:state.adminToken,
          fromDate:document.getElementById('slotFrom').value,
          toDate:document.getElementById('slotTo').value,
          capacity:Number(document.getElementById('slotCapacity').value || 4),
          startTime:document.getElementById('slotStart').value,
          endTime:document.getElementById('slotEnd').value,
          intervalMinutes:Number(document.getElementById('slotInterval').value || 60),
          includeWeekends:document.getElementById('slotWeekend').checked
        });
        showToast(`Đã tạo ${data.created || 0} khung giờ mới.`);
      }catch(error){handleAdminError(error)}finally{setLoading(false)}
    }

    function handleAdminError(error){
      showToast(error.message);
      if(['SESSION_EXPIRED','UNAUTHORIZED'].includes(error.code)) logoutAdmin();
    }

    function setLoading(show,text='Đang xử lý...'){
      document.getElementById('loadingText').textContent=text;
      document.getElementById('loading').classList.toggle('hidden',!show);
    }

    let toastTimer;
    function showToast(message){
      const toast=document.getElementById('toast');
      toast.textContent=message;
      toast.classList.add('show');
      clearTimeout(toastTimer);
      toastTimer=setTimeout(()=>toast.classList.remove('show'),3600);
    }

    function openModal(id){document.getElementById(id).classList.add('show')}
    function closeModal(id){document.getElementById(id).classList.remove('show')}

    function escapeHtml(value){
      return String(value ?? '').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
    }
    function escapeAttr(value){return escapeHtml(value).replace(/`/g,'&#96;')}
  </script>
</body>
</html>
